# NestJS and TypeORM rely on decorator metadata resolved through hoisted deps;
# strict isolation breaks their peer resolution at runtime.
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
enable-pre-post-scripts=true
